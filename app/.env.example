# PUBLIC ENV VARS — Sepolia
# Static values below are the current Interfold Sepolia deployment.
# Fill DAO + plugin addresses + deployment block from the `make deploy` output.

# General
NEXT_PUBLIC_DAO_ADDRESS=                                                  # from deploy output
NEXT_PUBLIC_TOKEN_ADDRESS=0xB5ECa717Db50010d195fd6908cdBF4589DA08094      # FOLD (timestamp-clock ERC20Votes)
NEXT_PUBLIC_INTERFOLD_FEE_TOKEN_ADDRESS=0xb743cDE9fbC72Ba06654267d1970be72A4Ea1445  # Interfold testnet fee token
NEXT_PUBLIC_FAUCET_ADDRESS=0x47Ef2F9764623Fd6154F389BA5Ccc54874F6EeD3  # testnet faucet: faucet() drips FOLD + fee token
NEXT_PUBLIC_ENABLE_FAUCET=true  # testnet only — leave unset/false in production

# Plugin addresses
# CRISP plugin -> PRIVATE (encrypted) proposals
NEXT_PUBLIC_CRISP_VOTING_PLUGIN_ADDRESS=                                  # from deploy output (index 0)
# TokenVoting plugin -> PUBLIC proposals (FOLD ERC20Votes token)
NEXT_PUBLIC_TOKEN_VOTING_PLUGIN_ADDRESS=                                  # from deploy output (index 1)
# Staged Proposal Processor (SPP) instances — proposals are created on these,
# which auto-create the stage-0 sub-proposal on the voting body and hold a
# stage-1 foundation veto window before execution.
NEXT_PUBLIC_SPP_PRIVATE_ADDRESS=                                          # SPP wrapping the CRISP body (from deploy output)
NEXT_PUBLIC_SPP_PUBLIC_ADDRESS=                                           # SPP wrapping the TokenVoting body (from deploy output)

NEXT_PUBLIC_SECONDS_PER_BLOCK=12

# The block where the DAO/plugins were deployed (speeds up event queries)
NEXT_PUBLIC_PLUGIN_DEPLOYMENT_BLOCK=                                      # from deploy output
# The block FOLD (InterfoldToken) was deployed at — start of the delegate-list event scan
NEXT_PUBLIC_TOKEN_DEPLOYMENT_BLOCK=10939866

# Interfold — hosted CRISP server
NEXT_PUBLIC_CRISP_SERVER_URL="https://crisp-api.theinterfold.com"
NEXT_PUBLIC_CRISP_PROGRAM_ADDRESS=0x12f7a216aEaB6620dC3488754970b66c63ECdD2C  # Crisp.sol — on-chain round data

# Network and services
NEXT_PUBLIC_CHAIN_NAME=sepolia
NEXT_PUBLIC_WEB3_ENDPOINT=https://eth-sepolia.g.alchemy.com/v2/           # your Alchemy/Infura key

NEXT_PUBLIC_WALLET_CONNECT_PROJECT_ID="YOUR WALLET CONNECT PROJECT ID"

NEXT_PUBLIC_IPFS_ENDPOINTS="https://domain1/api,https://domain2/api/v0"
# SERVER-ONLY (no NEXT_PUBLIC_ prefix): read by pages/api/ipfs/pin.ts.
# A NEXT_PUBLIC_ prefix would inline this into the client bundle.
PINATA_JWT="..."

# SERVER-ONLY: read by pages/api/etherscan.ts.
ETHERSCAN_API_KEY="OPTIONAL: ETHERSCAN API"

# Optional — have sensible defaults in constants.ts; override only if needed
# NEXT_PUBLIC_PROJECT_URL="https://theinterfold.com/"
# NEXT_PUBLIC_CRISP_INFO_URL="https://docs.theinterfold.com/"
# NEXT_PUBLIC_MINIMUM_START_DELAY_IN_SECONDS=30
